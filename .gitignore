# macOS
.DS_Store

# Claude Code local (personal, machine-specific + ephemeral worktrees)
.claude/settings.local.json
.claude/worktrees/
.superpowers/

# Rust
Backend/target/
**/target/
# Backend/Cargo.lock is committed intentionally (workspace ships binaries)

# Node / SvelteKit
Frontend/node_modules/
Frontend/.svelte-kit/
Frontend/build/
Frontend/.env
Frontend/.env.*
!Frontend/.env.example

# Env / secrets
.env
.env.*
!.env.example
secrets/

# Docker file-secrets (master key etc.) — never commit these
Docker/secrets/

# sqlx offline cache is committed intentionally (Fase 2+): .sqlx/
